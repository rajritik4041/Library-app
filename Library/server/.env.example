# MongoDB (optional). Agar blank / connect fail → FILE mode (Excel catalog + JSON issues)
MONGODB_URI=

# 1 = hamesha FILE mode (Mongo nahi) — teacher login + issues local files se
USE_FILE_STORE=1

JWT_SECRET=change-this-to-a-long-random-string
PORT=3001
DEFAULT_TEACHER_ID=T001
DEFAULT_TEACHER_PASSWORD=teacher123
DEFAULT_TEACHER_NAME=Library Teacher
