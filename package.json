{
  "name": "library-app",
  "private": true,
  "description": "Monorepo root — Render: Root Directory KHALI rakhein, Build & Start default",
  "scripts": {
    "postinstall": "node scripts/sync-backend.js --no-import && npm install --prefix backend",
    "start": "npm start --prefix backend",
    "sync-backend": "node scripts/sync-backend.js"
  }
}
